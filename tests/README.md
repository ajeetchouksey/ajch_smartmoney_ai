# Tests

This directory will contain integration and end-to-end tests for SmartMoney Copilot.

## Structure (planned)

```
tests/
├── integration/   # API integration tests
├── e2e/           # End-to-end UI tests (Playwright / Cypress)
└── agents/        # Agent workflow tests
```

Unit tests live alongside their respective source files in `src/`.
